spring.application.name=smartinvoice-backend

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/smartinvoice_db
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Server Configuration
server.port=8081

# Frontend URL - used to build the public invoice links emailed to clients
smartinvoice.frontend-url=http://localhost:5173

# JWT Configuration
# Do NOT commit this secret to a public repo in a real project - move it to an env var.
jwt.secret=/Sk3FNm7vBjhnaTh3QJ2gP8voM76M2GgZootDTZ9vMDvUxUbc2b23yeOutS9wqG/nblXtRCSqscx0NqI5vuA0Q==
jwt.expiration-ms=86400000

# Mail Configuration (used for recurring invoice and payment reminder emails)
# Fill in real SMTP credentials to send mail. The reminder job still runs and logs what it
# would have sent (rather than failing) if these are left blank.
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username= ashiknayem163@gmail.com
spring.mail.password= upkrrurokjyjqsfg
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.from= ashiknayem163@gmail.com

# Recurring Invoices & Payment Reminders
# Cron format: second minute hour day-of-month month day-of-week
smartinvoice.recurring.cron=0 0 2 * * *
smartinvoice.reminders.cron=0 0 8 * * *
smartinvoice.reminders.upcoming-days-before=3
smartinvoice.reminders.overdue-repeat-days=7